build/
install/

# User-specific environment: contains absolute paths to this server's spack
# view, install prefixes, scratch folders, hostname. Copy setup_env.example.sh
# → setup_env.sh and edit the paths for your machine.
setup_env.sh

# Local-only docs: machine-specific build/recompile notes (paths, OptiX SDK,
# driver/ABI details). Kept out of the committed README.
*.local.md

# Runtime artefacts produced by edep-sim / eic-opticks (often contain absolute
# paths in run_meta.txt header rows).
*.npy
*.root
*.gdml
run_meta.txt
log.txt
*.log

# Editor / OS noise
.vscode/
.idea/
*.swp
.DS_Store
